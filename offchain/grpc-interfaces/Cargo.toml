[package]
name = "grpc-interfaces"
version = "0.9.1"
edition = "2021"

[dependencies]
tonic = "0.9"
prost = "0.11"

[build-dependencies]
tonic-build = "0.9"

[package.metadata.cargo-machete]
ignored = ["prost"]
