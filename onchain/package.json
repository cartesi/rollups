{
    "private": true,
    "scripts": {
        "prettier:rollups": "yarn workspace @cartesi/rollups prettier",
        "prettier:rollups-cli": "yarn workspace @cartesi/rollups-cli prettier"
    },
    "workspaces": [
        "rollups",
        "rollups-cli"
    ],
    "devDependencies": {
        "prettier": "^3"
    }
}
